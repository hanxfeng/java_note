- # 打包

	- spring 项目可以生成基于 java 环境运行的 jar 包，双击 mvn-生存期-package 即可，生成的 jar 包存放于 target 目录下
	
	- 如果直接生成 jar 包，那么在生成时会对测试类中方法进行测试，产生的数据也会打包进 jar 包中，建议使用`跳过测试模式`进行测试
	
	- 运行命令为：`java -jar 包名.jar`
	
	- 使用 maven 打包要加一个插件，不过在生成 spring 项目时会自动添加，知道即可

