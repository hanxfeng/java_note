- 概述：
	- Redis 是一种数据库，其中的数据以键值对的形式存储，value 部分可以是一个 json 格式的数据，也可以是 List/Set 集合，哈希表等
- NoSQL：
	- 与关系型数据库 SQL 不同，NoSQL 是指非关系型数据库
	- 对比：![[SQL与NoSQL的对比.png]]
- Redis 特征：
	- 键值型，value 支持多种不同数据结构
	- 单线程，所有命令串行执行，命令具有原子性
	- 低延迟，速度快（基于内存，IO多路复用，良好编码）
	- 支持数据持久化
	- 支持主从集群，分片集群
	- 支持多语言客户端